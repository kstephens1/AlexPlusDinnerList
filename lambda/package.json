{
  "name": "alexa-plus-dinner-list-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda backend for the Dinnertime widget skill.",
  "main": "index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --check index.js && node --test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.817.0"
  }
}
